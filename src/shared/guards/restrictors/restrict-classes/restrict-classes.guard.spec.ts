import { RestrictClassesGuard } from './restrict-classes.guard';

describe('RestrictClassesGuard', () => {
  it('should be defined', () => {
    expect(new RestrictClassesGuard()).toBeDefined();
  });
});
